---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chartpaper-api-ingress
  namespace: apps 
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - tools.ashudev.in
      secretName: chartpaper-tls
  rules:
    - host: tools.ashudev.in
      http:
        paths:
          - path: /chartpaper/api
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chartpaper-ui-ingress
  namespace: apps
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
    - hosts:
        - tools.ashudev.in
      secretName: chartpaper-tls
  rules:
    - host: tools.ashudev.in
      http:
        paths:
          - path: /chartpaper
            pathType: Prefix
            backend:
              service:
                name: ui
                port:
                  number: 80

